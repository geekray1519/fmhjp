name: FMHY月次データ更新

on:
  schedule:
    # 毎月1日 午前0時 UTC (午前9時 JST)
    - cron: '0 0 1 * *'
  workflow_dispatch: # 手動実行用

permissions:
  contents: write
  issues: write

jobs:
  update-fmhy-data:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1

      - name: Node.js セットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: 依存関係インストール
        run: npm ci

      - name: FMHYデータを取得・パース
        id: fetch
        run: |
          set -o pipefail
          node scripts/update-data.mjs 2>&1 | tee update-output.log
          echo "success=true" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: データ取得失敗時のレポート
        if: steps.fetch.outputs.success != 'true'
        run: |
          echo "::error::FMHY データ取得に失敗しました"
          cat update-output.log || true
          exit 1

      - name: 変更を確認・差分レポート
        id: check
        run: |
          git add src/data/resources/ src/data/category-meta.json src/data/index.ts
          if git diff --staged --quiet; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "変更なし — データは最新です"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "変更差分サマリー:"
            git diff --staged --stat
            AFTER=$(grep -r '"name"' src/data/resources/ | wc -l)
            echo "リソース数: $AFTER"
            echo "diff_summary<<EOF" >> $GITHUB_OUTPUT
            git diff --staged --stat >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi

      - name: ビルド確認
        if: steps.check.outputs.changed == 'true'
        run: npm run build

      - name: コミット＆プッシュ
        if: steps.check.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          DATE=$(date +'%Y-%m-%d')
          RESOURCE_COUNT=$(grep -r '"name"' src/data/resources/ | wc -l)
          git commit -m "📦 FMHYデータ月次更新 - $DATE ($RESOURCE_COUNT リソース)"
          git push
